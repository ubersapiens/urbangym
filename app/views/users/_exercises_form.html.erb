<%= form_tag add_reps_user_path(current_user) do %>
  	<% @user_exercises.each do |user_exercise| %>
	    <%= user_exercise.exercise.name %>
	    <%= number_field_tag "exercises[#{user_exercise.exercise.id}]" %>
	    <br />
	<% end %>
  <%= submit_tag %>
<% end %>