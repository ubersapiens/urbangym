<p>
<%= form_tag add_reps_user_path(current_user) do %>
	<table>
		<tbody>
			<% @exercises.each do |exercise| %>
			<tr>
				<td style="width: 200px"><%= exercise.name %></td>
				<td style="width: 30px"><%= number_field_tag "exercises[#{exercise.id}]" %></td>
			</tr>
			<% end %>
		</tbody>
	</table>
	<%= submit_tag %>
<% end %>
</p>