<a class="btn btn-large btn-success" href="<%= edit_profile_user_path(current_user) %>">Edit Profile</a>
<h1><font size='5'>You are not alone...Here's a list of Urban Monkeys.</font></h1>
<table>
	<thead></thead>
	<tbody>
		<% for user in @users %>
		 <tr>
		 	<td><%= link_to user.name, user_path(user)%></td>
		 </tr>
		<% end %>
	</tbody>
</table>
