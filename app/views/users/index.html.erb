<a class="btn btn-large btn-success" href="<%= edit_profile_user_path(current_user) %>">Edit Profile</a>
<a class="btn btn-large btn-success" href="<%= exercises_path %>">Track Progress</a>
<h1>You are not alone...</h1>
<h2>Here's a list of Urban Monkeys.
<table>
	<thead></thead>
	<tbody>
		<% for user in @users %>
		 <tr>
		 	<td><%= link_to user.name, user_path(user)%></td>
		 </tr>
		<% end %>
	</tbody>
</table>
