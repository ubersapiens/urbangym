<a class="btn btn-large btn-success" href="<%= new_exercise_path %>">Add an Exercise</a> 
<h1><font size='5'>Read about each move.</font></h1>
<table>
	<thead></thead>
	<tbody>
		<% for exercise in @exercises %>
		 <tr>
		 	<td><%= link_to exercise.name, exercise_path(exercise)%></td>
		 </tr>
		<% end %>
	</tbody>
</table>
