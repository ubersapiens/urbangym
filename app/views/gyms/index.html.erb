<a class="btn btn-large btn-success" href="<%= new_gym_path %>">Add UrbanGym</a>
<h2>Here's a list of the top gyms in town.
<table>
	<thead></thead>
	<tbody>
		<% for gym in @gyms %>
		 <tr>
		 	<td><%= link_to gym.full_street_address, gym_path(gym)%></td>
		 </tr>
		<% end %>
	</tbody>
</table>
